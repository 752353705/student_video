function gameTime(t1,t2) {
		// var sec_cmt = msg.slice(0, 3)
		// return sec_cmt
		
		if(t2.split(' ')[0].split('-')[0] - t1.split(' ')[0].split('-')[0] < 1){
			return true
		}else {
			return false
		}
		
	}
	module.exports = {
		gameTime: gameTime,
	}